/**
 * ビューポートとレイアウトのデフォルト値
 */
export const DEFAULT_VIEWPORT = {
  /** デフォルトのビューポート幅（px） */
  WIDTH: 1920,
  /** デフォルトのビューポート高さ（px） */
  HEIGHT: 1080,
  /** デフォルトのフォントサイズ（px） */
  FONT_SIZE: 16
} as const;