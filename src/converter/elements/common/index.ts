/**
 * 要素共通ユーティリティのエクスポート
 */

export {
  DEFAULT_EMBEDDED_SIZE,
  parseSize,
  isValidUrl,
  getBorder,
  getBorderRadius,
  type SizeableAttributes,
  type StyleableAttributes,
} from "./embedded-element-utils";
