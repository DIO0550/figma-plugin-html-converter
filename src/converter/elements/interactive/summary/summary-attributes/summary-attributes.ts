import type { GlobalAttributes } from "../../../base/global-attributes";

/**
 * summary要素の属性インターフェース
 * summary要素は独自の属性を持たず、グローバル属性のみを使用
 * @see https://developer.mozilla.org/ja/docs/Web/HTML/Element/summary
 */
export type SummaryAttributes = GlobalAttributes;
