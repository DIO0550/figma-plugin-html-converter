import type { GlobalAttributes } from "../../../base/global-attributes";

/**
 * ul要素の属性定義
 * HTMLのul要素で使用可能な属性を定義します
 * ul要素はグローバル属性のみをサポートし、要素固有の属性はありません
 */
export type UlAttributes = GlobalAttributes;
