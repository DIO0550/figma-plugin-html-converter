import type { GlobalAttributes } from "../../../base/global-attributes";

/**
 * mark要素の属性定義
 * HTMLのmark要素で使用可能な属性を定義します
 * mark要素は特別な属性を持たず、グローバル属性のみをサポートします
 */
export type MarkAttributes = GlobalAttributes;
