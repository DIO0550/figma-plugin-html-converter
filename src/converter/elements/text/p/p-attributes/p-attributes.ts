import type { GlobalAttributes } from "../../../base/global-attributes";

/**
 * p要素の属性定義
 * HTMLのp要素で使用可能な属性を定義します
 * p要素はグローバル属性のみをサポートし、要素固有の属性はありません
 */
export type PAttributes = GlobalAttributes;
