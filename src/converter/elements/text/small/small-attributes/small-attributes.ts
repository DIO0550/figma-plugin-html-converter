import type { GlobalAttributes } from "../../../base/global-attributes";

/**
 * small要素の属性定義
 * HTMLのsmall要素で使用可能な属性を定義します
 * small要素は特別な属性を持たず、グローバル属性のみをサポートします
 */
export type SmallAttributes = GlobalAttributes;
