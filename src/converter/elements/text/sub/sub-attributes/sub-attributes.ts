import type { GlobalAttributes } from "../../../base/global-attributes";

/**
 * sub要素の属性定義
 * HTMLのsub（下付き文字）要素で使用可能な属性を定義します
 * sub要素は特別な属性を持たず、グローバル属性のみをサポートします
 */
export type SubAttributes = GlobalAttributes;
