import type { GlobalAttributes } from "../../../base/global-attributes";

/**
 * sup要素の属性定義
 * HTMLのsup（上付き文字）要素で使用可能な属性を定義します
 * sup要素は特別な属性を持たず、グローバル属性のみをサポートします
 */
export type SupAttributes = GlobalAttributes;
